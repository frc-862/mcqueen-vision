name: Javadoc

on: [push]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: actions/setup-java@v1
        with:
          java-version: 11
      - name: Generate Javadoc
        run: ./gradlew javadoc
      - name: Deploy VisionEngine Docs 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.1
        env:
          ACTIONS_ALLOW_UNSECURE_COMMANDS: true
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: gh-pages
          CLEAN: true
          FOLDER: VisionEngine/build/docs/javadoc
          TARGET_FOLDER: javadoc/VisionEngine/*
      - name: Deploy Pipelines Docs 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.1
        env:
          ACTIONS_ALLOW_UNSECURE_COMMANDS: true
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: gh-pages
          CLEAN: true
          FOLDER: Pipelines/build/docs/javadoc
          TARGET_FOLDER: javadoc/Pipelines/*
      - name: Deploy Processing Docs 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.1
        env:
          ACTIONS_ALLOW_UNSECURE_COMMANDS: true
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: gh-pages
          CLEAN: true
          FOLDER: Processing/build/docs/javadoc
          TARGET_FOLDER: javadoc/Processing/*
          